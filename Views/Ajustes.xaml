<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Divisas.Views.Ajustes"
             Title="Ajustes">

    <VerticalStackLayout Padding="20">
        <Label Text="Ajustes"
               FontSize="Large"
               HorizontalOptions="Center"/>

        <TableView Intent="Form"
                   BackgroundColor="Transparent">
            <TableRoot>
                <TableSection Title="Información del Usuario">
                    <TextCell Text="Nombre"
                              Detail="{Binding Usuario.Nombre}"/>
                    <TextCell Text="Apellido"
                              Detail="{Binding Usuario.Apellido}"/>
                    <TextCell Text="Correo"
                              Detail="{Binding Usuario.Correo}"/>
                    <TextCell Text="Rol"
                              Detail="{Binding Usuario.Rol}"/>
                </TableSection>
            </TableRoot>
        </TableView>
    </VerticalStackLayout>

</ContentPage>